# Ambiente virtual
venv/
env/
.venv/

# Cache do Python
__pycache__/
*.pyc

# Configurações sensíveis — NUNCA versionar
.env

# Banco de dados local (SQLite)
db.sqlite3
*.sqlite3

# Arquivos de mídia (uploads) e estáticos coletados
media/
staticfiles/

# Migrations geradas automaticamente NÃO devem ser ignoradas
# (o Django precisa delas — deixamos essa linha só como lembrete, sem comentar nada)

# Sistema operacional / editores
.DS_Store
.vscode/
.idea/
